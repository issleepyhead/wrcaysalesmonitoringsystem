<UserControl x:Class="WrcaySalesInventorySystem.Custom.ProductEntity"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WrcaySalesInventorySystem.Custom"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="100">
    <Grid Background="{StaticResource ApplicationLight}">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Sample Product"
                   VerticalAlignment="Center"/>
        <UniformGrid Grid.Row="1" Rows="1" Columns="3">
            <Button BorderBrush="{Binding null}"
                    Content="+"/>
            <hc:TextBox BorderBrush="SlateGray"
                        BorderThickness=".5"
                        Width="30"
                        Height="20"
                        hc:BorderElement.CornerRadius="0"/>
            <Button BorderBrush="{Binding null}"
                    Content="+"/>
        </UniformGrid>
    </Grid>
</UserControl>
